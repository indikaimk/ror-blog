<h1>New Article</h1>

<%= form_with model: @article do |form| %>
  <fieldset>
    <%= form.label :title %>
    <%= form.text_field :title %>
  </fieldset>
  <fieldset>
    <%= form.rich_text_area :content %>
  </fieldset>
  <%= form.submit "Save" %>
<% end %>